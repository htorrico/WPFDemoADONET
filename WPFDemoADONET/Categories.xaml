<Window x:Class="WPFDemoADONET.Categories"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFDemoADONET"
        mc:Ignorable="d"
        Title="Categories" Height="450" Width="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="Gray" BorderThickness="1" Padding="15" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Nombre:" Grid.Row="0" Grid.Column="0" Margin="0,0,10,10" VerticalAlignment="Center"/>
                <TextBox x:Name="txtName" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" Height="28"/>

                <TextBlock Text="Descripción:" Grid.Row="1" Grid.Column="0" Margin="0,0,10,10" VerticalAlignment="Center"/>
                <TextBox x:Name="txtDescription" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" Height="28"/>

                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Content="Guardar" Width="100" Height="30" Margin="0,0,10,0" Click="BtnGuardar_Click"/>
                    <Button Content="Limpiar" Width="100" Height="30" Margin="0,0,10,0" />
                    <Button Content="Listar" Width="100" Height="30" Click="BtnListar_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <TextBlock Grid.Row="1" Text="Listado de categorías"
                   FontSize="16" FontWeight="Bold" Margin="0,15,0,10"/>

        <DataGrid x:Name="dgCategories"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="0,0,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding IdCategory}" Width="80"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
